import { QueueManagerOptions } from '../queue.manager.options';
import { QueueAbstract } from '../queue.abstract';
export declare abstract class JobAbstract {
    protected options: QueueManagerOptions;
    constructor(options: QueueManagerOptions);
    abstract listen(queues: QueueAbstract, callback?: Function): any;
    listenAll(queues: QueueAbstract[], callback?: Function): Promise<void>;
}
